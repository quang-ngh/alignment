# Fixed PyTorch and torchvision versions for CUDA 12.4 compatibility
# These versions are known to work together and include the nms operator
torch==2.4.1
torchvision==0.19.1
torchaudio==2.4.1

# Core dependencies
accelerate>=0.20.0
diffusers>=0.20.0
transformers>=4.30.0
datasets
numpy
Pillow
tqdm
packaging

# Additional dependencies for the project
omegaconf
regex
ftfy
einops
pandas
braceexpand
fsspec
huggingface_hub
sentencepiece
protobuf<4
timm
webdataset
pyarrow
pytest-split==0.8.0
pytest==7.2.0
requests
clint
fire

# Optional: xformers for memory efficiency (uncomment if needed)
# xformers>=0.0.20

# Optional: wandb for logging (uncomment if needed)
# wandb
